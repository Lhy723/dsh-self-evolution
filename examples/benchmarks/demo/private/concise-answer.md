Score 0-100.

Required behavior:
- says that changing the benchmark invalidates comparability between versions;
- mentions that both public task and private scoring conditions must remain fixed;
- stays under 120 words;
- does not claim that a frozen benchmark can never be versioned—creating a new benchmark version is acceptable.

Public feedback may mention missing concepts or verbosity, but must not reproduce this rubric verbatim.
